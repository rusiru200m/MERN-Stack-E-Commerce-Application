name: Build and Deploy

on:
  push:
    branches:
      - feature/ci-cd-for-dev
      - develop
      - main

permissions:
  contents: read
  packages: write

env:
  GHCR_NAMESPACE: rusiru200m

jobs:
  determine_environment:
    name: Determine Environment
    runs-on: ubuntu-latest

    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
      image_tag: ${{ steps.set-env.outputs.image_tag }}

    steps:
      - name: Determine environment
        id: set-env
        shell: bash
        run: |

          case "${GITHUB_REF_NAME}" in

            feature/ci-cd-for-dev)
              ENVIRONMENT="dev"
              ;;

            develop)
              ENVIRONMENT="staging"
              ;;

            main)
              ENVIRONMENT="prod"
              ;;

            *)
              echo "Unsupported branch: ${GITHUB_REF_NAME}"
              exit 1
              ;;

          esac

          IMAGE_TAG="${ENVIRONMENT}-${GITHUB_SHA::7}"

          echo "Environment: ${ENVIRONMENT}"
          echo "Image tag: ${IMAGE_TAG}"

          echo "environment=${ENVIRONMENT}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"



  validate:
    name: Validate required files
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate required files
        run: |
          test -f backend/Dockerfile
          test -f frontend/Dockerfile
          test -f docker-compose.yml


  
  build_push:
    name: Build and Push Images
    runs-on: ubuntu-latest

    needs:
      - determine_environment
      - validate

    environment:
      name: ${{ needs.determine_environment.outputs.environment }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Show deployment information
        run: |
          echo "Branch: $GITHUB_REF_NAME"
          echo "Environment: ${{ needs.determine_environment.outputs.environment }}"
          echo "Image tag: ${{ needs.determine_environment.outputs.image_tag }}"

     
      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.GHCR_NAMESPACE }}/ecommerce-backend:${{ needs.determine_environment.outputs.image_tag }}

     
      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.GHCR_NAMESPACE }}/ecommerce-frontend:${{ needs.determine_environment.outputs.image_tag }}


  
  # deploy:
  #   name: Deploy to AKS
  #   runs-on: ubuntu-latest

  #   needs:
  #     - determine_environment
  #     - build_push

  #   environment:
  #     name: ${{ needs.determine_environment.outputs.environment }}

  #   steps:

  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     # ----------------------------------------------
  #     # Azure Login
  #     # ----------------------------------------------
  #     - name: Azure Login
  #       uses: azure/login@v2
  #       with:
  #         creds: |
  #           {
  #             "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
  #             "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
  #             "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
  #             "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}"
  #           }

  #     # ----------------------------------------------
  #     # Get AKS credentials
  #     # ----------------------------------------------
  #     - name: Get AKS credentials
  #       run: |
  #         az aks get-credentials \
  #           --resource-group "${{ secrets.AZURE_RESOURCE_GROUP }}" \
  #           --name "${{ secrets.AKS_CLUSTER_NAME }}" \
  #           --overwrite-existing

  #     # ----------------------------------------------
  #     # Deploy Namespace
  #     # ----------------------------------------------
  #     - name: Deploy namespace
  #       run: |
  #         kubectl apply \
  #           -f k8s/${{ needs.determine_environment.outputs.environment }}/namespace.yaml

  #     # ----------------------------------------------
  #     # Deploy Application
  #     # ----------------------------------------------
  #     - name: Deploy application
  #       run: |

  #         ENVIRONMENT="${{ needs.determine_environment.outputs.environment }}"
  #         IMAGE_TAG="${{ needs.determine_environment.outputs.image_tag }}"

  #         BACKEND_IMAGE="ghcr.io/${{ env.GHCR_NAMESPACE }}/ecommerce-backend:${IMAGE_TAG}"
  #         FRONTEND_IMAGE="ghcr.io/${{ env.GHCR_NAMESPACE }}/ecommerce-frontend:${IMAGE_TAG}"

  #         echo "Environment: ${ENVIRONMENT}"
  #         echo "Backend: ${BACKEND_IMAGE}"
  #         echo "Frontend: ${FRONTEND_IMAGE}"

  #         sed \
  #           -e "s|BACKEND_IMAGE_PLACEHOLDER|${BACKEND_IMAGE}|g" \
  #           -e "s|FRONTEND_IMAGE_PLACEHOLDER|${FRONTEND_IMAGE}|g" \
  #           k8s/${ENVIRONMENT}/deployment.yaml \
  #           | kubectl apply -f -

  #     # ----------------------------------------------
  #     # Deploy Services
  #     # ----------------------------------------------
  #     - name: Deploy services
  #       run: |
  #         kubectl apply \
  #           -f k8s/${{ needs.determine_environment.outputs.environment }}/service.yaml

  #     # ----------------------------------------------
  #     # Verify deployment
  #     # ----------------------------------------------
  #     - name: Verify rollout
  #       run: |

  #         ENVIRONMENT="${{ needs.determine_environment.outputs.environment }}"

  #         kubectl rollout status \
  #           deployment/ecommerce-backend \
  #           -n ecommerce-${ENVIRONMENT} \
  #           --timeout=180s

  #         kubectl rollout status \
  #           deployment/ecommerce-frontend \
  #           -n ecommerce-${ENVIRONMENT} \
  #           --timeout=180s

  #     - name: Show Kubernetes resources
  #       run: |
  #         ENVIRONMENT="${{ needs.determine_environment.outputs.environment }}"

  #         kubectl get pods -n ecommerce-${ENVIRONMENT}
  #         kubectl get svc -n ecommerce-${ENVIRONMENT}